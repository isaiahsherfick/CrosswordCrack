CrosswordHack

CrosswordHack is a CLI utility that assists in solving crosswords by allowing you to search an English dictionary using pattern matching, keyword definition search, and thesaurus search.

Example:

crosshack --length 4 --pattern **s* --


Simple queries can be combined with tools like grep to trim results to great success.
